{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability set for Phase 1.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
